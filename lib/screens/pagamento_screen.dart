import 'package:flutter/cupertino.dart';

class PagamentoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("PAGAMENTO"));
  }
}
